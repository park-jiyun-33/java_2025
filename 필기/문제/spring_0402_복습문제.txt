1. [spring test] SPRING에서 JunitTest를 하려고 한다.
    pom에서 다운로드 받을때 필요한 것은? ( junit, spring-context, spring-test )

2. [mysql 연동] SPRING에서 MYSQL를 연동하려고 한다.
   pom에서 다운로드 받을때 필요한 것은? ( myslq-connector-java, spring-jdbc )

3. [mybatis 연동] spring에서 mybatis를 사용하려고 한다.
   pom에서 다운로드 받을때 필요한 것은? ( mybatis, mybatis-spring )

4. root-context.xml 에서 DataSource를 연동하려고 한다. 
   필요한 클래스 이름은? ( SimpleDriverDataSource )

5. 위의 클래스에서 Setter를 이용하여 Property를 지정하려고 한다.
    property 이름을 적으시오. (  driverClass, url, username, password  )

6. mybatis에 ( sqlSessionFactory )는 sqlSession을 생성 및 관리, ( sqlSession ) 은 sql을 실행한다. 












